# Git Summary

一款基于 Electron 的 Git 提交记录汇总工具，帮助你快速生成工作周报。

## 功能特性

- 📁 支持本地 Git 仓库浏览和选择
- 🔄 支持多仓库、多分支同时管理
- 📅 按时间范围筛选提交记录
- 👤 按作者筛选提交记录
- 🤖 AI 智能生成周报（支持阿里云通义千问）
- 📝 内置 Markdown 编辑器
- 💾 一键复制周报内容

## 下载安装

前往 [Releases 页面](https://github.com/zygzyg2/git-summary/releases) 下载对应平台的安装包：

| 平台 | 文件格式 |
|------|----------|
| Windows | `.zip` (解压后运行 `GitSummary.exe`) |
| macOS | `.zip` (解压后运行 `GitSummary.app`) |
| Linux | `.AppImage` (添加执行权限后直接运行) |

### Linux 使用方法

```bash
chmod +x GitSummary-x.x.x-linux-x86_64.AppImage
./GitSummary-x.x.x-linux-x86_64.AppImage
```

### macOS 注意事项

首次打开可能提示"无法验证开发者"，请前往 **系统偏好设置 > 安全性与隐私** 点击"仍要打开"。

## 使用说明

### 1. 添加仓库

点击「选择仓库」按钮，浏览并选择本地的 Git 仓库目录。

### 2. 筛选提交记录

- 选择时间范围（默认本周）
- 选择分支（支持多选，或选择"所有分支"）
- 选择作者（可选）

### 3. 获取提交记录

点击「获取提交记录」按钮，加载符合条件的 Git 提交。

### 4. 生成周报

- **手动编辑**：在编辑器中直接编写周报
- **AI 生成**：配置 AI 设置后，点击「AI 优化周报」自动生成

### 5. AI 配置（可选）

点击设置图标，配置阿里云通义千问 API：

1. 获取 API Key：[阿里云百炼控制台](https://bailian.console.aliyun.com/)
2. 填入 API Key
3. 选择模型（推荐 qwen-plus）

## 开发指南

### 环境要求

- Node.js >= 18
- npm >= 9

### 安装依赖

```bash
npm install
```

### 开发模式

```bash
npm run dev:electron
```

### 构建打包

```bash
# 打包所有平台
./build-all.sh

# 或单独打包
npm run build:electron
```

## 技术栈

- React + TypeScript
- Vite
- Electron
- Ant Design

## License

MIT
